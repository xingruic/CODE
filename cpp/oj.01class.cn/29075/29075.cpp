#include<bits/stdc++.h>
using namespace std;
int m, n;
int t[501];
int main() {
    cin >> n >> m;
    for (int i = 1; i <= n; i++) cin >> t[i];
    sort(t + 1, t + n + 1);
    int ans = 0;
    int pos=1,val=t[1];
    while (true) {
        
        for (int i = 1; i <= n; i++)
    }
}